#include "Spelunk.hpp"
#include "WorldGen.hpp"

int main (int argc, char *argv[]) {
    // RoomNode node(1, RoomNodeType::JUNCTION, RoomNodeDepth::LOWER);
    WorldGen gen(20, 20, 50);
    
    return 0;
}
